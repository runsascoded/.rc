#!/usr/bin/env bash

# Node
append_to NODE_PATH .

export jsh="$s/js-helpers"

append_to_path "$HOME/node_modules/.bin"

if which brew &> /dev/null; then
  export NVM_DIR=~/.nvm
  . $(brew --prefix nvm)/nvm.sh
fi

defn nr npm run

defn ppj pretty-print-json.js
