#!/usr/bin/env bash

# Grep options
export USE_LIBPCRE=yes
alias grp="ggrep"

export GREP_OPTIONS="--color=auto"

alias pcg=pcregrep
alias pgrp=pcregrep

alias po="pcregrep -o"
alias pgo="pcregrep -o"
alias pcgo="pcregrep -o"

alias gv="grep -v"
alias v="grep -v"

alias dg="dict-grep"
alias dgs="dict-grep-subsequence"

alias gro="grep -o"
alias grn="grep -n"
alias grni="grep -ni"

alias gm1="grep -m 1"

alias gp=grep
alias ge=grep

