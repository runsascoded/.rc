#!/usr/bin/env bash

# Grep options
export USE_LIBPCRE=yes

#export GREP_OPTIONS="--color=auto"

unalias grep &> /dev/null
grep="$(which grep)"
#ggrep="$(which ggrep 2> /dev/null)"

#if [ -n "$ggrep" ]; then
#  defn grep $ggrep --color=auto
#else
  defn grep "$grep" --color=auto
#fi

defn pg pcregrep
defn pgi pcregrep -i
defn po pcregrep -o
defn pgo pcregrep -o
defn por pcregrep -or
defn pro pcregrep -or

defn gv grep -v
defn v grep -v

defn dg dict-grep
defn dgs dict-grep-subsequence

defn r grep
defn rl grep -l
defn rrl grep -rl
defn ro grep -o
defn rr grep -r
defn rro grep -ro
defn rc grep -c
defn rrc grep-rc
defn rn grep -n
defn rrn grep -rn
defn rv grep -v
defn ri grep -i
defn rri grep -ri
defn rir grep -ri
defn rni grep -ni
defn rvc grep -vc
defn rcv grep -cv

defn rbn grep -n --binary-files text
defn rnb grep -n --binary-files text

defn r1 grep -m1
defn r2 grep -m2
defn r3 grep -m3
defn r4 grep -m4
defn r5 grep -m5
defn r10 grep -m10

defn ro1 grep -om1
defn ro2 grep -om2
defn ro3 grep -om3
defn ro4 grep -om4
defn ro5 grep -om5
defn ro10 grep -om10

defn re grep-e
defn rve grep-v-e
defn rce grep-e -c
defn rec grep-e -c
defn rvce grep-e -c -v

defn ra1 grep -A1
defn ra2 grep -A2
defn ra3 grep -A3
defn ra4 grep -A4
defn ra5 grep -A5
defn ra10 grep -A10
defn ra20 grep -A20

defn rA1 grep -A1
defn rA2 grep -A2
defn rA3 grep -A3
defn rA4 grep -A4
defn rA5 grep -A5
defn rA10 grep -A10
defn rA20 grep -A20

defn rB1 grep -B1
defn rB2 grep -B2
defn rB3 grep -B3
defn rB4 grep -B4
defn rB5 grep -B5
defn rB10 grep -B10
defn rB20 grep -B20

defn rC1 grep -C1
defn rC2 grep -C2
defn rC3 grep -C3
defn rC4 grep -C4
defn rC5 grep -C5
defn rC10 grep -C10
defn rC20 grep -C20
