#!/usr/bin/env bash

# Grep options
export USE_LIBPCRE=yes
alias grp="ggrep"
export GREP_OPTIONS="--color=auto"
alias pgrp="pcregrep"

alias gv="grep -v"

alias dg="dict-grep"
alias dgs="dict-grep-subsequence"
