#!/usr/bin/env bash

# Navigation aliases
alias cdp="cd $PWD"

alias u="cd .."
alias uu="cd ../.."
alias uuu="cd ../../.."
alias uuuu="cd ../../../.."

alias psh="pushd"
alias pop="popd"
alias P="pushd"
alias b="popd"

alias mcd=multi-cd
