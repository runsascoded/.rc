#!/usr/bin/env bash

alias edp=explode-parent-dirs

rsync-only() {
  arg="$1"
  shift
  rsync -avzh --include="$arg" --exclude='*' "$@"
}
defn rso rsync-only

rsync-only-n() {
  arg="$1"
  shift
  rsync-only "$arg" -n "$@"
}
defn rson rsync-only-n
