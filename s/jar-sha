#!/bin/bash

set -e
jar-shas "$1" | shasum | col 0
#jar="$(realpath "$1")"
#
#tmpdir="$(tmpfile)"
#mkdir "$tmpdir"
#cd "$tmpdir"
#jar xf "$jar"
#find . | xargs shasum 2> /dev/null | shasum
#
#rm -rf "$tmpdir"
