There's different escaping required when setting bash-color variables for setting up my prompt/$PS1 than when exposing
color variables generally for bash-script-colored-output use, so `bash_colors` and `prompt_colors` are slightly
different, accordingly.
